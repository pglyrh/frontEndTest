<!doctype html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <meta name="Generator" content="EditPlus®">
  <meta name="Author" content="">
  <meta name="Keywords" content="">
  <meta name="Description" content="">
  
  <title>dom window——跳转</title>
	
  <script type="text/javascript">
/*
  //删除记录按钮
  function conf(){
	//确认框，有确定（True）、取消（False）两个按钮
	var res = confirm("确定删除？");
	//确认结果
	if(res){
		alert("true");
	}else{
		alert("false");
	}
  }
	
	function sayHello(){
		alert("hello");
	}
	//循环调用某个函数（根据指定时间周期(以ms为单位)）
	//setInterval("sayHello()",10000000);

	//制作简单时钟
	function showTime(){
		//document.writeln(new Date());
		//在元素间的文本通过对象.innerHTML访问
		document.getElementById("time").innerHTML = new Date().toLocaleString();
		document.getElementById("time2").innerText= new Date().toLocaleString();
	}
	//循环调用某个函数
	setInterval("showTime()",1000);

	//指定时间(以ms为单位)）后，调用某个函数
	//setTimeout("sayHello()",1000);
	
	//新窗口句柄
	var new_window;
	//打开新窗口
	function openNew(){
		//window.open(URL,name,features,replace)
		new_window = open("function.html","_blank");
	}
	//向新窗口传递消息
	function send(){
		//new_window实际是新窗口的引用，将信息传递给子窗口
		new_window.document.getElementById("span1").innerText = "父窗口的消息来了";
	}
	*/
  </script>
 </head>
 <body>
 <div id="log">
	<!-- 表单 -->
	<form action="a.html" method="get">
		<!-- input宽度可直接用size定义，或采用css的width属性 -->
		<div style="margin:15px 15px 15px 15px">
			<span >用户名：</span><input type="text" id="usr" size="20"/><br>
		</div>
		<div style="margin:15px 15px 15px 15px">
			<span >密&nbsp;&nbsp;&nbsp;&nbsp;码：</span><input type="password" id="pwd" size="21"/><br>
		</div>
		<div style="margin:15px 15px 15px 70px">
			<input type="submit" value="确认" id="sure"/>
			<input type="reset" value="重置" id="reset"/>
		</div>
		
	</form>	
 </div>
 </body>
</html>
